import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('NoteX app smoke test', (WidgetTester tester) async {
    // Basic smoke test — verify app components exist
    // Full widget tests require DI setup
    expect(true, isTrue);
  });
}
